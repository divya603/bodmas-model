# BODMAS project — agent instructions

1. **Read `HANDOFF.md` first.** It is the complete, current orientation for this project
   (model, human experiment, LLM experiment, Bayesian baseline, prereg, results plan).
2. **Keep `HANDOFF.md` current.** As you complete work (scripts, figures, findings, decisions,
   payments, deploys), update its relevant sections incrementally, or at the latest before the
   session ends. The next session must be able to pick up cold from HANDOFF.md alone.
3. Writing style for anything user-facing: no em dashes. LaTeX is compiled on Overleaf only
   (self-contained folders, figures referenced as `figs/<exact-filename>`); never install a
   local TeX toolchain. See HANDOFF.md §9 for the full gotcha list.
