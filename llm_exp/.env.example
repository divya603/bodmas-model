# Copy this file to `.env` and fill in your key. `.env` is gitignored — never commit it.
OPENROUTER_API_KEY=sk-or-...

# Optional OpenRouter attribution headers (show up on your OpenRouter dashboard).
OPENROUTER_APP_URL=
OPENROUTER_APP_NAME=bodmas-llm-exp
